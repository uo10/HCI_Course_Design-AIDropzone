# 复制为 .env.local 后生效（Vite 会读取）
# PowerShell: Copy-Item .env.example .env.local

# ── 模式 A：答辩 / 无后端（默认，可不建 .env.local）──
VITE_USE_MOCK=true

# ── 模式 B：完整桌面 — 推荐直接用 npm run electron:dev:full（读 .env.full）
# 若用手动配置，复制本文件为 .env.local 并取消下面注释：
# VITE_USE_MOCK=false
# VITE_API_BASE=http://127.0.0.1:8000
